using UnityEngine;

namespace DogShop.Shop
{
    /// <summary>
    /// 계산대. 클릭 대상을 표시하는 마커다 — 레이캐스트가 이 컴포넌트를 찾아 발주 메뉴를 연다.
    /// 태그·이름 비교를 쓰지 않기 위해 컴포넌트로 둔다.
    /// </summary>
    public class ShopCounter : MonoBehaviour
    {
    }
}
